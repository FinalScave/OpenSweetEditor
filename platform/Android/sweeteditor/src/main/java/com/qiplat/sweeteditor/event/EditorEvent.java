package com.qiplat.sweeteditor.event;

/**
 * Base class for editor events.
 * <p>
 * Each concrete event is an immutable data Bean that inherits from this class.
 * New events can be defined by creating a new subclass in the event package,
 * without modifying existing code.
 */
public abstract class EditorEvent {
}
